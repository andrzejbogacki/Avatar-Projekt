'use strict';

// Moduł 3 — Volt Token: 1 token na Avatara, alokacja procentowa płynna.
module.exports = Object.freeze({
    SUMA_ALOKACJI_MAKS_PROCENT: 100,
    MIN_PROCENT: 0, // wartości > 0; zero usuwa pozycję przy redystrybucji
});
